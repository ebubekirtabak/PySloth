class ScopeModel:

    def __init__(self, settings, search_item, page, reporting):
        self.settings = settings
        self.search_item = search_item
        self.page = page
        self.reporting = reporting

